先把整個檔案放到C槽
全部都是終端指令
終端依序執行下面指令進虛擬環境(每一次都要跑)

cd "C:\RL_Projects"
.\sc2_new_env\Scripts\activate
cd "C:\RL_Projects\my_agent"
$env:SC2PATH = "D:\Game\StarCraft II"

 #這個要看你的星海在哪裡 指到資料夾就好

到這邊已經在虛擬環境裡了．接著是下載套件(跑過第一次較好)  

終端執行下面指令先下載這些(執行一次之後不用再執行了)     #版本衝突問一下gemini 我具體版本有些忘了

pip install pysc2 torch absl-py tensorboard
pip install "protobuf==3.20.3" "numpy==1.23.5"

這邊可以跑AI生log下面都是指令 
log出來都在.\RL_Projects\my_agent\logs裡面



python train_hybrid.py    #跑AI
python -m tensorboard.main --logdir=logs  #看log的指令 會是本地的網址

python train_ppo.py ＃不用理他
python pvt_agent_basic.py  # 執行杜明道的腳本的指令
python pvt_agent_fight.py  # 執行楊以振的腳本的指令
